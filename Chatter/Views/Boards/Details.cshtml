@model Chatter.Models.Board

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Board</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
        </dt>
       
        <dd>
            @foreach (var blog in Model.Blogs)
            {
            <div>
         
                @blog.Title;
                
            </div> 

            }
         </dd>
</dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
